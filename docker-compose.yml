services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: codespace_backend
    restart: unless-stopped
    ports:
      - "8001:8001"
    env_file:
      - ./backend/.env
    environment:
      - PORT=8001
